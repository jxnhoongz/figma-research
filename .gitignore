node_modules/
dist/
.DS_Store
*.local

# TypeScript build artifacts (from tsc -b composite project)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Local MCP config — contains a Figma token, never commit
.mcp.json

# Raw Figma plugin export dump (22MB) — curated assets live in src/assets/section2
src/assets/figma-plugin/

# Unpacked plugin export bundles (throwaway) — curated assets are copied into
# src/assets/<section> by scripts/build-section-scene.mjs
figma-export-section*/
figma-export/

# Raw screen recordings (huge, often >100MB → can't push). The committed demo
# is the compressed mp4 in demo/; keep originals local.
demo/*.mov
